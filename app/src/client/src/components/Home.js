import React from "react";

const Home = () => {
    return (
        <div>
            Welcome to Parasect! Pick an app from the navbar above to get started.
        </div>
    );
};

export default Home;