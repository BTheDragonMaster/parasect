import React from "react";

const Home = () => {
    return (
        <div>
            Welcome to Paras! Pick an app from the navigation bar above to get started.
        </div>
    );
};

export default Home;